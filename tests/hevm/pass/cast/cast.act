contract B

contract A

constructor
interface constructor(uint x0)

iff
    CALLVALUE == 0

creates
    uint x := x0


transition getx
interface getx()

iff

  CALLVALUE == 0

returns pre(x)


transition setx
interface setx(uint x1)

iff

  CALLVALUE == 0

storage
  x := x1


constructor
interface constructor(address<A> x, address<A> y)

iff
    CALLVALUE == 0
    x =/= y

creates
    A a1 := x
    A a2 := y


transition upd
interface upd()

iff

  CALLVALUE == 0

storage
  a1.x := 42
  a2.x := 11
