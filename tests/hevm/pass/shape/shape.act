contract B

contract A

// contract A
constructor
interface constructor(uint z)

iff
   CALLVALUE == 0

creates
   uint x := z


transition x
interface x()

iff
   CALLVALUE == 0

returns
   pre(x)

transition set_x
interface set_x(uint z)

iff 
   CALLVALUE == 0

storage
  x := z

// contract B
constructor
interface constructor(uint z)
iff
   CALLVALUE == 0

creates
   uint y := z
   A a := create A(0)

transition y
interface y()

iff
   CALLVALUE == 0

returns
   pre(y)

transition a
interface a()

iff
   CALLVALUE == 0

returns
   pre(a)


// contract C

contract C

constructor
interface constructor(address<B> y)

iff
   CALLVALUE == 0

creates
   A a := y.a
   B b := y 

transition change
interface change()

iff

  CALLVALUE == 0
  
storage
  b.a.x := 17
