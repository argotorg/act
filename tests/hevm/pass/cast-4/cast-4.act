contract C

constructor(uint z1)

creates
    uint z := z1

contract A

constructor(uint x1)

creates
    uint x := x1
    C c := new C(42)


contract B

constructor(address<A> x, address<A> y)

iff
    address(x) != address(y)
    // address(x.c) != address(y.c) -- Do we not need this?

creates
    A a1 := x
    A a2 := y

transition upd()

updates

    a1 := new A(42)
    a2 := new A(43)
