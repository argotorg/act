contract Map

constructor()

creates
  uint128 val := 11
  mapping(uint => uint128) f := [11 => 42]
  //mapping(uint => uint256) f := [11 => 42]

transition val() : uint128

updates
returns val

transition f(uint x) : uint128

updates
returns f[x]


transition set(uint128 val, uint key) : uint256

updates

  f := f[key => val]

returns 1
