contract Map

constructor()

creates
  uint128 val := 11
  mapping(uint => uint128) f := [11 => 42, 42 => 1]
  mapping(uint128 => bool) g := [22 => true, 1 => false]
  

transition val() : uint128

updates
returns val

transition f(uint x) : uint128

updates
returns f[x]


transition g(uint128 x) : bool

updates
returns g[x]



transition setf(uint128 val, uint key) : uint256

updates

  f := f[key => val]

returns 1


transition setg(bool val,  uint128 key) : uint256

iff

  CALLVALUE == 0

updates

  g := g[key => val]

returns 1
