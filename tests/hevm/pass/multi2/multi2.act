contract B

contract A

constructor
interface constructor(uint z)

iff
   CALLVALUE == 0
   inRange(uint256, z + 1)

creates
   uint x := z + 1


transition x
interface x()

iff
   CALLVALUE == 0

returns
   pre(x)

transition set_x
interface set_x(uint z)

iff
   CALLVALUE == 0

storage
  x := z

constructor
interface constructor(uint u)

iff
   CALLVALUE == 0
   inRange(uint256, u + 1)

creates
   uint y := 0
   A a := create A(u)