contract A

constructor(uint z)

iff
   inRange(uint256, z + 1)

creates
   uint x := z + 1


transition x() : uint256

updates
returns
   x

transition set_x(uint z)

updates
  x := z

contract B

constructor(uint u)

iff
   inRange(uint256, u + 1)

creates
   uint y := 0
   A a := new A(u)
