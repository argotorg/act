constructor of A
interface constructor()
iff
  CALLVALUE == 0

creates
  mapping(uint64 => mapping(uint128 => uint32)) m := []


behaviour setm of A
interface setm(uint64 a, uint128 b, uint32 c)

iff
  CALLVALUE == 0

storage
  m[a][b] => c


behaviour m of A
interface m(uint64 idx1, uint128 idx2)

iff CALLVALUE == 0

returns pre(m[idx1][idx2])
