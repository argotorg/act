contract Map

constructor()

creates
  uint256 val := 0
  mapping(uint => uint) f := [0 => 42]

transition val() : uint256

updates
returns val

transition f(uint x) : uint256

updates
returns f[x]


transition set(uint val, uint key) : uint256

iff

  CALLVALUE == 0

updates

  f := f[key => val]

returns 1
